So what i want is a clone of the site FluenType. Write a webapp using vue and tailwind and a simple database that like FluentType uses AI to do language lerning. FluentType is inspired by MoneyTyp where your given a test and you need to type it as fast as posible. the twist with fluenttype is that you need to wirte a translated version of the text in you traget language. 

How it should work is that you create an account and a profile/language where you enter your skill level, then it will give you your first text (generated using ai) you can either enter the full text to get points that will then give you even harder senteces or you can practis where you write word by word without instantly loosing but not getting any points. Completet Text are saved and retestet like anki does it with known cards and leand cards. The better you get the longer and harder the sentences get. Words also get stored, so you start with a small vocab and the ai will intreduce some new words sometimes that will then be stores again. most words should be known words and the user should be able to add words that the have learnd manualy. So track a skill value and use that do prompt the ai for new sentences defining how long the sentece should be, how hard and what words to use.